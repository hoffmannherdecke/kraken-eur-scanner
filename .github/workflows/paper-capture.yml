name: Kraken paper market capture

'on':
  workflow_dispatch:
  schedule:
    - cron: '22 * * * *'
  pull_request:
    paths:
      - 'paper_capture/**'
      - 'market_data/**'
      - '.github/workflows/paper-capture.yml'
  push:
    branches: [main]
    paths:
      - 'paper_capture/**'
      - 'market_data/**'
      - '.github/workflows/paper-capture.yml'

# Public market data only. No exchange secrets, write permissions or messaging.
permissions:
  contents: read
  actions: read

jobs:
  verify:
    if: github.event_name != 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      - run: python -m pip install websockets==15.0.1
      - name: Controlled sequence, shared market data and parity tests
        run: |
          python -m unittest discover -s paper_capture -p 'test_*.py' -v
          python -m unittest discover -s market_data -p 'test_*.py' -v
      - name: Live Kraken EUR book and trade probe
        run: python paper_capture/market.py --seconds 60 --out probe --smoke
      - name: Verify Kraken EUR candle and ticker context
        run: |
          python - <<'PY'
          import json, sys
          sys.path.insert(0, 'paper_capture')
          from market import rest_market_context
          ctx = rest_market_context('RAY/EUR', 'RAYEUR')
          assert ctx['ticker']['last'] > 0
          assert ctx['candles']['1m'] and ctx['candles']['5m'] and ctx['candles']['15m'] and ctx['candles']['60m']
          assert ctx['levels']['support_15m'] is not None and ctx['levels']['resistance_15m'] is not None
          print('REST_CONTEXT_VERIFIED ' + json.dumps({
              'symbol': ctx['symbol'],
              'retrieved_at': ctx['retrieved_at'],
              'ticker': ctx['ticker'],
              'levels': ctx['levels'],
              'volume_acceleration_5m_vs_prior20m': ctx['volume_acceleration_5m_vs_prior20m'],
              'latest_1m': ctx['candles']['1m'][-1],
              'latest_5m': ctx['candles']['5m'][-1],
              'latest_15m': ctx['candles']['15m'][-1],
              'latest_60m': ctx['candles']['60m'][-1],
          }, sort_keys=True))
          PY
      - name: Verify scanner market discovery and live subscriptions
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python paper_capture/market.py --seconds 75 --out discovery-probe
          python - <<'PY'
          import gzip, json
          rows = [json.loads(line) for line in gzip.open('discovery-probe/events.jsonl.gz', 'rt')]
          assert any(r['kind']=='discovery_ok' for r in rows), 'candidate discovery unverified'
          assert not any(r['kind'] in ('discovery_error','subscription_error','trade_sequence_gap') for r in rows), 'hard capture error'
          observed = [r for r in rows if r['kind']=='scanner_market_observed']
          verified = {r['symbol'] for r in rows if r['kind']=='book_verified'}
          assert observed, 'scanner market discovery empty'
          assert all(r['symbol'] in verified for r in observed), 'observed market book missing'

          gaps = [r for r in rows if r['kind']=='data_gap']
          recovered = False
          if gaps:
              last_gap_seq = max(r['seq'] for r in gaps)
              assert any(r['kind']=='market_data_incomplete' and r.get('component')=='websocket'
                         and r['seq'] > last_gap_seq for r in rows), 'gap not quality-labelled'
              assert any(r['kind']=='connection_start' and r['seq'] > last_gap_seq for r in rows), 'gap not reconnected'
              assert any(r['kind']=='book_verified' and r['seq'] > last_gap_seq for r in rows), 'book not restored after gap'
              assert any(r['kind']=='micro_snapshot' and r.get('quality')=='complete'
                         and r['seq'] > last_gap_seq for r in rows), 'no complete post-gap recovery window'
              recovered = True

          print('PAPER_DISCOVERY_VERIFIED', json.dumps({
              'observed':len(observed),'verified_symbols':sorted(verified),
              'gaps':len(gaps),'recovered_after_gap':recovered
          }))
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: paper-market-probe-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            probe/
            discovery-probe/
          retention-days: 90
          if-no-files-found: error

  capture:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 80
    # Deliberately no shared concurrency lock: hourly sessions must overlap.
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      - run: python -m pip install websockets==15.0.1
      - name: Continuous public market capture
        env:
          GH_TOKEN: ${{ github.token }}
        run: python paper_capture/market.py --seconds 4200 --out capture
      - name: Split evidence into bounded downloadable parts
        if: always()
        run: |
          python - <<'PY'
          import hashlib
          import json
          import shutil
          from pathlib import Path

          source = Path('capture/events.jsonl.gz')
          manifest = Path('capture/manifest.json')
          if not source.is_file() or not manifest.is_file():
              raise SystemExit('capture evidence missing')

          out = Path('capture-parts')
          out.mkdir(exist_ok=True)
          # Keep every capture within the 41 upload slots defined below.
          # Scale the chunk size to the actual archive instead of silently
          # producing un-uploaded tail parts when market volume is high.
          max_parts = 41
          min_part_size = 24 * 1024 * 1024
          source_size = source.stat().st_size
          needed_part_size = (source_size + max_parts - 1) // max_parts
          mib = 1024 * 1024
          needed_part_size = ((needed_part_size + mib - 1) // mib) * mib
          part_size = max(min_part_size, needed_part_size)
          whole_hash = hashlib.sha256()
          parts = []
          with source.open('rb') as src:
              index = 0
              while True:
                  block = src.read(part_size)
                  if not block:
                      break
                  whole_hash.update(block)
                  name = f'events.jsonl.gz.part{index:03d}'
                  path = out / name
                  path.write_bytes(block)
                  parts.append({
                      'index': index,
                      'name': name,
                      'size_bytes': len(block),
                      'sha256': hashlib.sha256(block).hexdigest(),
                  })
                  index += 1

          if len(parts) > max_parts:
              raise SystemExit(f'capture produced {len(parts)} parts but only {max_parts} upload slots exist')

          shutil.copy2(manifest, out / 'manifest.json')
          parts_manifest = {
              'schema_version': 1,
              'source_name': source.name,
              'source_size_bytes': source.stat().st_size,
              'source_sha256': whole_hash.hexdigest(),
              'part_size_bytes': part_size,
              'parts': parts,
              'reassembly': 'concatenate parts in ascending index order, then verify source_size_bytes and source_sha256',
          }
          (out / 'parts-manifest.json').write_text(
              json.dumps(parts_manifest, sort_keys=True, separators=(',', ':')) + '\n',
              encoding='utf-8',
          )
          print('PAPER_CAPTURE_PARTS', json.dumps(parts_manifest, sort_keys=True))
          PY
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/parts-manifest.json') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-manifest
          path: |
            capture-parts/manifest.json
            capture-parts/parts-manifest.json
          compression-level: 9
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part000') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-000
          path: capture-parts/events.jsonl.gz.part000
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part001') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-001
          path: capture-parts/events.jsonl.gz.part001
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part002') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-002
          path: capture-parts/events.jsonl.gz.part002
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part003') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-003
          path: capture-parts/events.jsonl.gz.part003
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part004') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-004
          path: capture-parts/events.jsonl.gz.part004
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part005') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-005
          path: capture-parts/events.jsonl.gz.part005
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part006') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-006
          path: capture-parts/events.jsonl.gz.part006
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part007') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-007
          path: capture-parts/events.jsonl.gz.part007
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part008') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-008
          path: capture-parts/events.jsonl.gz.part008
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part009') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-009
          path: capture-parts/events.jsonl.gz.part009
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part010') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-010
          path: capture-parts/events.jsonl.gz.part010
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part011') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-011
          path: capture-parts/events.jsonl.gz.part011
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part012') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-012
          path: capture-parts/events.jsonl.gz.part012
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part013') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-013
          path: capture-parts/events.jsonl.gz.part013
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part014') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-014
          path: capture-parts/events.jsonl.gz.part014
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part015') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-015
          path: capture-parts/events.jsonl.gz.part015
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part016') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-016
          path: capture-parts/events.jsonl.gz.part016
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part017') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-017
          path: capture-parts/events.jsonl.gz.part017
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part018') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-018
          path: capture-parts/events.jsonl.gz.part018
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part019') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-019
          path: capture-parts/events.jsonl.gz.part019
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part020') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-020
          path: capture-parts/events.jsonl.gz.part020
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part021') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-021
          path: capture-parts/events.jsonl.gz.part021
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part022') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-022
          path: capture-parts/events.jsonl.gz.part022
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part023') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-023
          path: capture-parts/events.jsonl.gz.part023
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part024') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-024
          path: capture-parts/events.jsonl.gz.part024
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part025') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-025
          path: capture-parts/events.jsonl.gz.part025
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part026') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-026
          path: capture-parts/events.jsonl.gz.part026
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part027') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-027
          path: capture-parts/events.jsonl.gz.part027
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part028') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-028
          path: capture-parts/events.jsonl.gz.part028
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part029') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-029
          path: capture-parts/events.jsonl.gz.part029
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part030') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-030
          path: capture-parts/events.jsonl.gz.part030
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part031') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-031
          path: capture-parts/events.jsonl.gz.part031
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part032') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-032
          path: capture-parts/events.jsonl.gz.part032
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part033') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-033
          path: capture-parts/events.jsonl.gz.part033
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part034') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-034
          path: capture-parts/events.jsonl.gz.part034
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part035') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-035
          path: capture-parts/events.jsonl.gz.part035
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part036') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-036
          path: capture-parts/events.jsonl.gz.part036
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part037') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-037
          path: capture-parts/events.jsonl.gz.part037
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part038') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-038
          path: capture-parts/events.jsonl.gz.part038
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part039') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-039
          path: capture-parts/events.jsonl.gz.part039
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        if: always() && hashFiles('capture-parts/events.jsonl.gz.part040') != ''
        with:
          name: paper-market-tape-${{ github.run_id }}-${{ github.run_attempt }}-part-040
          path: capture-parts/events.jsonl.gz.part040
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
